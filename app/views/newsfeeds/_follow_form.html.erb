<% unless signed_in? %>
    <div id="follow_form">
      <%# if current_user.following?(newsfeed.id) %>
          <%= render 'unfollow' %>
      <%#else %>
          <%= render 'follow' %>

    </div>
<% end %>